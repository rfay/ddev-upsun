plugin.plugin_configuration.opensearch_connector.basicauth:
  type: plugin.plugin_configuration.opensearch_connector.standard
  label: 'OpenSearch basic authentication connector settings'
  mapping:
    username:
      type: string
      label: 'Username for HTTP basic authentication'
    password:
      type: string
      label: 'Password for HTTP basic authentication'
